name: CodeQL

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron: "31 7 * * 3"

permissions:
  actions: read
  checks: write
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        language:
          - typescript

    steps:
      - name: Checkout
        id: checkout
<<<<<<< HEAD
<<<<<<< HEAD
        uses: actions/checkout@v6
||||||| parent of ca8c0be (chore(deps): bump actions/checkout from 4 to 5)
        uses: actions/checkout@v4
=======
        uses: actions/checkout@v5
>>>>>>> ca8c0be (chore(deps): bump actions/checkout from 4 to 5)
||||||| parent of 94ae688 (chore(deps): bump actions/checkout from 5 to 6)
        uses: actions/checkout@v5
=======
        uses: actions/checkout@v6
>>>>>>> 94ae688 (chore(deps): bump actions/checkout from 5 to 6)

      - name: Initialize CodeQL
        id: initialize
        uses: github/codeql-action/init@v4
        with:
          config-file: .github/codeql/codeql-config.yml
          languages: ${{ matrix.language }}
          source-root: src

      - name: Autobuild
        id: autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        id: analyze
        uses: github/codeql-action/analyze@v4
